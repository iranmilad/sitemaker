<div>
    <div class="blog-img mb-3">
        <img class="rounded-0 blur-up lazyload"
            data-src="{{ asset($post->image) }}"
            src="{{ asset($post->image) }}"
            alt="{{ $post->title }}" width="1200" height="700" />
    </div>
</div>
